// This file is auto generated by icy-table.
#ifndef TABLE_COMPILER_INDEX
#define TABLE_COMPILER_INDEX
#define array_element_size(array) sizeof(array[0])
#define array_count(array) (sizeof(array)/array_element_size(array))
#include "icydb.h"
#include <stdlib.h>
#endif


pid_lookup * pid_lookup_create(const char * optional_name){
  static const char * const column_names[] = {(char *)"key"};
  static const char * const column_types[] = {"pid"};
  pid_lookup * instance = calloc(sizeof(pid_lookup), 1);
  instance->column_names = (char **)column_names;
  instance->column_types = (char **)column_types;
  
  icy_table_init((icy_table * )instance, optional_name, 1, (unsigned int[]){sizeof(pid)}, (char *[]){(char *)"key"});
  
  return instance;
}

void pid_lookup_insert(pid_lookup * table, pid * key, size_t count){
  void * array[] = {(void* )key};
  icy_table_inserts((icy_table *) table, array, count);
}

void pid_lookup_set(pid_lookup * table, pid key){
  void * array[] = {(void* )&key};
  icy_table_inserts((icy_table *) table, array, 1);
}

void pid_lookup_lookup(pid_lookup * table, pid * keys, size_t * out_indexes, size_t count){
  icy_table_finds((icy_table *) table, keys, out_indexes, count);
}

void pid_lookup_remove(pid_lookup * table, pid * keys, size_t key_count){
  size_t indexes[key_count];
  size_t index = 0;
  size_t cnt = 0;
  while(0 < (cnt = icy_table_iter((icy_table *) table, keys, key_count, NULL, indexes, array_count(indexes), &index))){
    icy_table_remove_indexes((icy_table *) table, indexes, cnt);
    index = 0;
  }
}

void pid_lookup_clear(pid_lookup * table){
  icy_table_clear((icy_table *) table);
}

void pid_lookup_unset(pid_lookup * table, pid key){
  pid_lookup_remove(table, &key, 1);
}

bool pid_lookup_try_get(pid_lookup * table, pid * key){
  void * array[] = {(void* )key};
  void * column_pointers[] = {(void *)table->key};
  size_t __index = 0;
  icy_table_finds((icy_table *) table, array[0], &__index, 1);
  if(__index == 0) return false;
  unsigned int sizes[] = {sizeof(pid)};
  for(int i = 1; i < 1; i++){
    if(array[i] != NULL)
      memcpy(array[i], column_pointers[i] + __index * sizes[i], sizes[i]); 
  }
  return true;
}

void pid_lookup_print(pid_lookup * table){
  icy_table_print((icy_table *) table);
}

size_t pid_lookup_iter(pid_lookup * table, pid * keys, size_t keycnt, pid * optional_keys_out, size_t * indexes, size_t cnt, size_t * iterator){
  return icy_table_iter((icy_table *) table, keys, keycnt, optional_keys_out, indexes, cnt, iterator);

}
